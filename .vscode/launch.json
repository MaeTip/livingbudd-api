{
    "configurations": [

        {
            "type": "node-terminal",
            "name": "Run Dev",
            "request": "launch",
            "command": "yarn run start:dev",
            "cwd": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "Run Prod",
            "request": "launch",
            "command": "yarn run start:prod",
            "cwd": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "db:dev:restart",
            "request": "launch",
            "command": "yarn run db:dev:restart",
            "cwd": "${workspaceFolder}"
        }
    ]
}